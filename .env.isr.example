# ISR (Incremental Static Regeneration) Configuration
# Copy this file to .env.local and update with your values

# Revalidation Secret
# Used to secure the on-demand revalidation endpoint
# Generate with: openssl rand -base64 32
REVALIDATION_SECRET=your-secret-key-here

# Webhook Secret
# Used to verify webhook signatures from CMS
# Should match the secret configured in your CMS webhook settings
WEBHOOK_SECRET=your-webhook-secret-here

# Optional: Custom CLS endpoint for reporting
# If you want to send CLS data to an external service
# NEXT_PUBLIC_CLS_ENDPOINT=https://your-analytics-endpoint.com/cls

# ISR Cache Configuration (already in next.config.js)
# These are set in next.config.js but documented here for reference:
# - isrMemoryCacheSize: 50 MB (in-memory cache)
# - isrFlushToDisk: true (persist cache to disk)

# Revalidation Times (in seconds) - Reference
# These are hardcoded in pages but documented here:
# - Homepage: 900 (15 minutes)
# - Blog articles: 3600 (1 hour)
# - Quiz/test pages: 7200 (2 hours)
# - Error pages: 60 (1 minute)

# Example webhook URLs for different events:
# POST /api/webhook/content-update
# POST /api/revalidate?secret=<REVALIDATION_SECRET>

# Supported webhook event types:
# - article.created
# - article.updated
# - article.deleted
# - homepage.updated
# - schools.updated
# - faq.updated
# - quiz.updated
# - custom (with paths array)
# - bulk (with multiple articles)